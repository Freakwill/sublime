<snippet>
	<content><![CDATA[for parent, dirnames, filenames in os.walk(${1:rtdir}):
    for filename in filenames:
        path = os.path.join(parent, filename)]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>walk</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.python</scope>
	<description>Template of os.walk</description>
</snippet>